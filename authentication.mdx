---
title: Authentication (Sample)
description: Placeholder layout you can replace with your real authentication guide.
---

## Overview

<Callout kind="alert">
This page is a sample layout. Replace every credential, header, and flow with your real authentication guidance.
</Callout>

## Choose your auth model

<Tabs>
  <Tab title="Token" icon="key">
    Use this tab if your product uses static tokens or API keys.

    <Steps>
      <Step title="Create a token" icon="plus">
        Explain where users generate or receive credentials.
      </Step>
      <Step title="Send the header" icon="code">
        Show the simplest request that works.
      </Step>
    </Steps>

    ```bash
    curl https://api.yourcompany.com/v1/example \
      -H "Authorization: Bearer YOUR_TOKEN"
    ```

    <ParamField header="Authorization" param-type="string" required="true">
      `Bearer YOUR_TOKEN`
    </ParamField>
  </Tab>
  <Tab title="Session" icon="shield">
    Use this tab if your product authenticates with a login session.

    <Steps>
      <Step title="Sign in" icon="user">
        Describe the login page or SSO flow.
      </Step>
      <Step title="Confirm access" icon="check-circle">
        Explain how users verify they are authenticated.
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Common mistakes (sample)

<Expandable title="Why am I getting a 401?" default-open="false">
Double-check that the token is valid, not expired, and sent in the correct header.
</Expandable>